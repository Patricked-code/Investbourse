services:
  postgres:
    image: postgres:16-alpine
    container_name: investbourse-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: investbourse
      POSTGRES_PASSWORD: investbourse_password
      POSTGRES_DB: investbourse
    ports:
      - "5432:5432"
    volumes:
      - investbourse_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U investbourse -d investbourse"]
      interval: 10s
      timeout: 5s
      retries: 5

  api-gateway:
    build:
      context: ..
      dockerfile: docker/node-service.Dockerfile
      args:
        SERVICE_PATH: services/api-gateway
    container_name: investbourse-api-gateway
    restart: unless-stopped
    env_file:
      - ../.env.example
    environment:
      PORT: 4000
    ports:
      - "4000:4000"
    depends_on:
      postgres:
        condition: service_healthy
      contact-service:
        condition: service_started
      content-service:
        condition: service_started
      office-service:
        condition: service_started

  contact-service:
    build:
      context: ..
      dockerfile: docker/node-service.Dockerfile
      args:
        SERVICE_PATH: services/contact-service
    container_name: investbourse-contact-service
    restart: unless-stopped
    env_file:
      - ../.env.example
    environment:
      PORT: 4010
    ports:
      - "4010:4010"
    depends_on:
      postgres:
        condition: service_healthy

  content-service:
    build:
      context: ..
      dockerfile: docker/node-service.Dockerfile
      args:
        SERVICE_PATH: services/content-service
    container_name: investbourse-content-service
    restart: unless-stopped
    env_file:
      - ../.env.example
    environment:
      PORT: 4020
    ports:
      - "4020:4020"
    depends_on:
      postgres:
        condition: service_healthy

  office-service:
    build:
      context: ..
      dockerfile: docker/node-service.Dockerfile
      args:
        SERVICE_PATH: services/office-service
    container_name: investbourse-office-service
    restart: unless-stopped
    env_file:
      - ../.env.example
    environment:
      PORT: 4040
    ports:
      - "4040:4040"
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  investbourse_postgres_data:
